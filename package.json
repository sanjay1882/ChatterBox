{
    "name": "chatterbox-root",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\" \"python backend/Python-scripts/scraper.py\"",
        "dev": "npm start",
        "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:backend": "npm start --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}